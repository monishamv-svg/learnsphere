server {
    listen 8080;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback: /admin, /student, etc. → index.html for React Router
    location / {
        try_files $uri $uri/ /index.html;
    }
}
